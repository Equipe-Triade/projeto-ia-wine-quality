import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-wine-quality',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
